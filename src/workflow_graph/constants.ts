/**
 * constants.ts
 * Constants used throughout the workflow graph library.
 */

// Special node names
export const START: string = '__START__';
export const END: string = '__END__';

// Default values
export const DEFAULT_RETRY_COUNT: number = 0;
export const DEFAULT_RETRY_DELAY: number = 0.1;  // seconds 